# Asset Manager — 환경변수 설정
# 이 파일을 .env로 복사하고 실제 값으로 수정하세요.

# --- 필수 ---

# PostgreSQL 연결 문자열
# docker-compose 사용: postgresql://asset_manager:asset_manager_pass@postgres:5432/asset_manager
# EC2 호스트: postgresql://asset_manager:asset_manager_pass@172.17.0.1:5432/asset_manager
# 로컬 개발: postgresql://asset_manager:asset_manager_pass@localhost:5432/asset_manager
DATABASE_URL=postgresql://asset_manager:asset_manager_pass@postgres:5432/asset_manager

# Secure Cookie (HTTP 배포 시 false, HTTPS 배포 시 true)
SECURE_COOKIE=false

# Node 환경 (development 또는 production)
NODE_ENV=production

# 배치/스케줄러 인증 시크릿
CRON_SECRET=your-cron-secret-here

# --- 선택 ---

# Seed 관련 (개발/테스트 시에만 사용)
SEED_ADMIN_USERNAME=admin
SEED_ADMIN_PASSWORD=changeme123

# Slack 알림
SLACK_WEBHOOK_URL=

# SMTP (이메일 알림)
SMTP_HOST=
SMTP_PORT=587
SMTP_USER=
SMTP_PASS=
SMTP_FROM=
SMTP_SECURE=false
