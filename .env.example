# ── Database ─────────────────────────────────────────────────────────────────
# SQLite: 컨테이너 내부에서 볼륨 마운트 경로와 일치해야 함
# docker-compose 기준: /app/dev.db (볼륨: /home/ssm-user/app/data/dev.db:/app/dev.db)
DATABASE_URL="file:./dev.db"

# ── Seed: 초기 관리자 계정 ────────────────────────────────────────────────────
# npx prisma migrate reset 또는 npx prisma db seed 실행 시 사용
# 프로덕션 배포 후 반드시 변경하세요
SEED_ADMIN_USERNAME=admin
SEED_ADMIN_PASSWORD=changeme123
SEED_ADMIN_EMAIL=admin@example.com
SEED_ADMIN_NAME=관리자

# ── Google Workspace (선택) ───────────────────────────────────────────────────
# 사용하지 않는 경우 주석 처리
# GWS_ADMIN_EMAIL=admin@yourdomain.com
# GWS_DOMAIN=yourdomain.com
